# graphql-db-example

use method

```
$ git clone https://github.com/Otazoman/graphql-example.git
$ cd graphql-example
$ npm ci
$ npx prisma migrate dev --name sample
$ npx prisma generate
$ npm start
```
